<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0F172A"
    android:padding="16dp">

    <!-- Header Shield Status -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp"
        android:background="#1E293B">

        <ImageView
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@drawable/ic_launcher" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="شیلد گارد | آنتی ویروس هوشمند"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginTop="8dp"/>

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="سیستم امن است"
            android:textColor="#10B981"
            android:textSize="14sp"
            android:layout_marginTop="4dp"/>
    </LinearLayout>

    <!-- Scan Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/btnScanApps"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="اسکن برنامه‌ها"
            android:backgroundTint="#0EA5E9"/>

        <Button
            android:id="@+id/btnScanFiles"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="اسکن رسانه‌ها"
            android:layout_marginStart="6dp"
            android:backgroundTint="#0EA5E9"/>
    </LinearLayout>

    <Button
        android:id="@+id/btnCloudScan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="اسکن ابری (۷۰ آنتی‌ویروس جهانی)"
        android:layout_marginTop="6dp"
        android:backgroundTint="#8B5CF6"/>

    <!-- Results Display Area -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="12dp">

        <TextView
            android:id="@+id/resultText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="برای شروع اسکن یکی از گزینه‌های بالا را انتخاب کنید."
            android:textColor="#94A3B8"
            android:textSize="14sp"
            android:lineSpacingExtra="4dp"/>
    </ScrollView>
</LinearLayout>
